export default function  Page({params}){
    console.log({params})
    // console.log(params.slug)
    return <div>I am about page check console {params.val}</div>
    // return <div>My Post: {params.val}</div>
}